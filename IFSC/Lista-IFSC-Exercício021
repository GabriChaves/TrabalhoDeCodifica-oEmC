#include <stdio.h>

int main() {
    int A, B, resultado;

    printf("Digite A e B: ");
    scanf("%d %d", &A, &B);

    resultado = (A * A) + (B * B);

    printf("Soma dos quadrados: %d\n", resultado);

    return 0;
}
