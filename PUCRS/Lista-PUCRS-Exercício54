#include <stdio.h>

int main() {
    int a, b;
    printf("Dois numeros: ");
    scanf("%d %d", &a, &b);

    if (a == b) {
        printf("Numeros iguais.\n");
        return 0;
    }

    int passo = (b - a) / 3;
    printf("Divisao: %d, %d, %d, %d\n", a, a+passo, a+2*passo, b);
    return 0;
}
