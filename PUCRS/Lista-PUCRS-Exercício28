#include <stdio.h>

int main() {
    int n;
    float S = 0;

    printf("Digite um valor inteiro positivo: ");
    scanf("%d", &n);

    for (int i = 1; i <= n; i++) {
        S += 1.0 / i;
        printf("1/%d = %.4f\n", i, 1.0 / i);
    }

    printf("Valor de S: %.4f\n", S);
    return 0;
}
